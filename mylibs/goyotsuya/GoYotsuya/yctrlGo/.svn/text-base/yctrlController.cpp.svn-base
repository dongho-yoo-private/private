#include "yctrlController.h"

// ----------------------------------
cyctrlController::cyctrlController(void)
{
}

// ----------------------------------
cyctrlController::~cyctrlController(void)
{
}

// ----------------------------------
BOOL cyctrlController::OnPaint(HDC hdc)
{
	Graphics g(hdc);
	g.DrawRectangle(&Pen(Color::Black), 0, 0, m_w, m_h);
	return TRUE;
}

// ----------------------------------
BOOL cyctrlController::AddCommand(const char* pszText, int id)
{
	return TRUE;
}

// ----------------------------------
BOOL cyctrlController::AddCommand(Bitmap* bmp, int id)
{
	return TRUE;
}

// ---------------------------------------
BOOL cyctrlController::Create(HWND m_hWnd, int x, int y)
{
	if (cyctrlbase::Create(m_hWnd, NULL, eCI_CONTROLLER, x, y, 10, 10, TRUE)==FALSE)
	{
		return FALSE;
	}
}

// ---------------------------------------
void cyctrlController::Show(eControllerButtonSet btnSet)
{
	if (btnSet== )
	{
	}
	else if ()
	{
	}
	else if ()
	{
	}
}
